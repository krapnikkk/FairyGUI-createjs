
export const enum StateChangeEvent {
    CHANGED = "__stateChanged"
}

// export class StateChangeEvent extends createjs.Event{
    // constructor(type: string, bubbles: boolean, cancelable: boolean){
    //     super(type,bubbles,cancelable);

    // }
// }
